# Backend API base URL.
VITE_API_BASE_URL=http://localhost:5000

# Google Maps JavaScript API key, used to render the live map and Places
# Autocomplete. Leave blank to run with the built-in Melbourne CBD location
# picker instead (the app works fully without this key).
VITE_GOOGLE_MAPS_API_KEY=
